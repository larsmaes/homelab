apiVersion: azure.m.upbound.io/v1beta1
metadata:
  name: default
  nanespace: crossplane-system
kind: ProviderConfig
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: azure-credentials
      key: creds